---

compile_circom_circuits:
  n_repetitions: 1
  host_types:
    base:
      n: 1
      init_roles:
        - setup-base
        - setup-circom
      $CMD$: . {{ exp_code_dir }}/doe-suite-setup.sh && cd {{ exp_code_dir }}/circuits && ./bench_all.sh